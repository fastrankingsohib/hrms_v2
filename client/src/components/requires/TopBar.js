const TopBar = () => {
    return(
        <section className="w-full p-4 flex items-center">
            <input type="search" className="primary-search-bar" placeholder="Search" />
        </section>
    )
}

export default TopBar;